// Stub implementation for useAuth hook

export function useRegister() {
  return {
    register: async () => {},
    isLoading: false,
    error: null,
  };
}

export function useLogin() {
  return {
    login: async () => {},
    isLoading: false,
    error: null,
  };
}
